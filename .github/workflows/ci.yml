name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  # ── Job 1 : Lint & format du backend ──────────────────────────────────────
  lint-backend:
    name: Lint — Backend
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Installer les dépendances
        run: npm ci
        working-directory: backend

      - name: ESLint
        run: npx eslint . --ext .ts,.tsx
        working-directory: backend

      - name: Prettier — vérification du formatage
        run: npx prettier --check .
        working-directory: backend

  # ── Job 2 : Lint & format du frontend ─────────────────────────────────────
  lint-client:
    name: Lint — Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: client/package-lock.json

      - name: Installer les dépendances
        run: npm ci
        working-directory: client

      - name: ESLint
        run: npx eslint src --ext .ts,.tsx
        working-directory: client
        env:
          ESLINT_USE_FLAT_CONFIG: "false"

      - name: Prettier — vérification du formatage
        run: npx prettier --check src
        working-directory: client

  # ── Job 3 : Audit de sécurité ──────────────────────────────────────────────
  security:
    name: Security audit
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Audit backend
        run: npm audit --audit-level=critical
        working-directory: backend

      - name: Audit frontend
        run: npm audit --audit-level=critical
        working-directory: client

  # ── Job 4 : Build des images Docker ───────────────────────────────────────
  # Tourne seulement si lint-backend ET lint-client passent
  build:
    name: Docker build
    runs-on: ubuntu-latest
    needs: [lint-backend, lint-client]

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Configurer Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build — image backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build — image frontend
        uses: docker/build-push-action@v5
        with:
          context: ./client
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
